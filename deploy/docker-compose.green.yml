services:
  lumina-backend-green:
    image: ghcr.io/luminaerp360/lumina-backend:${IMAGE_TAG:-latest}
    container_name: lumina-backend-green
    restart: unless-stopped
    network_mode: host
    env_file:
      - ../apps/backend/.env
    environment:
      - NODE_ENV=production
      - PORT=3002
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--no-verbose",
          "--tries=1",
          "--spider",
          "http://localhost:3002/",
        ]
      interval: 30s
      timeout: 10s
      start_period: 40s
      retries: 3

  lumina-frontend-green:
    image: ghcr.io/luminaerp360/lumina-frontend:${IMAGE_TAG:-latest}
    container_name: lumina-frontend-green
    restart: unless-stopped
    ports:
      - "4002:80"
    networks:
      - lumina-network
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--no-verbose",
          "--tries=1",
          "--spider",
          "http://localhost:80/health",
        ]
      interval: 30s
      timeout: 10s
      start_period: 10s
      retries: 3

networks:
  lumina-network:
    name: lumina-network
    driver: bridge
